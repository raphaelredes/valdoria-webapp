var actionGuard=(()=>{'use strict';let _busy=false;let _lastTime=0;const DEBOUNCE_MS=300;const BTN_SELECTORS='[data-action],.action-btn,.btn-action,.btn-hero,.choice-btn,.v-btn';function _setButtonsDisabled(disabled){document.querySelectorAll(BTN_SELECTORS).forEach(b=>{if(disabled){b.style.pointerEvents='none';b.style.opacity='0.5';}else{b.style.pointerEvents='';b.style.opacity='';}});}
return{get busy(){return _busy;},acquire(){if(_busy)return false;const now=Date.now();if(now-_lastTime<DEBOUNCE_MS)return false;_lastTime=now;_busy=true;_setButtonsDisabled(true);return true;},release(){_busy=false;_setButtonsDisabled(false);},debounce(){const now=Date.now();if(now-_lastTime<DEBOUNCE_MS)return false;_lastTime=now;return true;},reset(){_busy=false;_setButtonsDisabled(false);}};})();