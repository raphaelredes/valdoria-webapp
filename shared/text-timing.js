function calcReadTime(text,category,dataElements){category=category||'overlay';dataElements=dataElements||0;var clean=(text||'').replace(/<[^>]*>/g,'').trim();var words=clean.split(/\s+/).filter(Boolean).length;var base=words*250;var ranges={'toast':{min:1500,max:4000,bonus:0},'toast-warn':{min:2000,max:5000,bonus:0},'overlay':{min:2000,max:5000,bonus:0},'summary':{min:2500,max:6000,bonus:1500},'narrative':{min:2500,max:5000,bonus:0},'data':{min:3000,max:5500,bonus:dataElements*800},'result':{min:4000,max:6000,bonus:1500},'dm':{min:2000,max:8000,bonus:0},'combat':{min:1500,max:4000,bonus:0},};var r=ranges[category]||ranges['overlay'];return Math.max(r.min,Math.min(r.max,base+r.bonus));}
if(typeof window!=='undefined')window.calcReadTime=calcReadTime;