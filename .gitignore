# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if ever used)
node_modules/
.tmp.driveupload/
.hash-manifest.json
.bundle-manifest.json
.wrangler/
*.css.bak

# Local PNG resize backups (never deploy — keeps originals safe locally)
_original/
_backup/
