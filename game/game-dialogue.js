var MOOD_ICONS={happy:'\u{1F60A}',angry:'\u{1F620}',sad:'\u{1F614}',busy:'\u{1F610}',neutral:'\u{1F5E3}'};var MOOD_LABELS={happy:'Amig\u00e1vel',angry:'Irritado',sad:'Melanc\u00f3lico',busy:'Ocupado',neutral:''};var MOOD_GRADIENTS={happy:'radial-gradient(ellipse at 50% 0%, #3a3225 0%, var(--v-bg) 70%)',angry:'radial-gradient(ellipse at 50% 0%, #352028 0%, var(--v-bg) 70%)',sad:'radial-gradient(ellipse at 50% 0%, #2a2a30 0%, var(--v-bg) 70%)',busy:'radial-gradient(ellipse at 50% 0%, #302c30 0%, var(--v-bg) 70%)',neutral:'radial-gradient(ellipse at 50% 0%, #3a3028 0%, var(--v-bg) 70%)',};var _twInterval=null;var _twCallback=null;var _twElement=null;var _twFullText='';function renderDialogue(screen){const contentEl=document.getElementById('content');if(!contentEl)return;const d=screen.dialogue;const mood=d.mood||'neutral';const moodClass='dlg-mood-'+mood;contentEl.innerHTML='';const card=document.createElement('div');card.className='dlg-card '+moodClass;const header=document.createElement('div');header.className='dlg-header';const iconSpan=document.createElement('span');iconSpan.className='dlg-mood-icon';iconSpan.textContent=MOOD_ICONS[mood]||MOOD_ICONS.neutral;const speakerSpan=document.createElement('span');speakerSpan.className='dlg-speaker';speakerSpan.textContent=d.speaker||'';header.appendChild(iconSpan);header.appendChild(speakerSpan);const moodLabel=MOOD_LABELS[mood];if(moodLabel){const labelSpan=document.createElement('span');labelSpan.className='dlg-mood-label';labelSpan.textContent=moodLabel;header.appendChild(labelSpan);}
card.appendChild(header);const body=document.createElement('div');body.className='dlg-body';card.appendChild(body);const skipHint=document.createElement('div');skipHint.className='dlg-skip-hint';skipHint.textContent='Toque para pular \u25B8';card.appendChild(skipHint);contentEl.appendChild(card);const dialogueText=screen.dialogue_text||extractDialogueText(screen.text||'');applyMoodAmbient(mood);typewriterDialogue(body,dialogueText,function(){skipHint.style.display='none';revealChoices();});card.addEventListener('click',function(){skipTypewriter();skipHint.style.display='none';},{once:true});}
function typewriterDialogue(el,text,onDone){if(window.vTypewriter)vTypewriter.skip();if(window.vTypewriter){vTypewriter.write(el,text,{cursorClass:'dlg-cursor',onDone:onDone});}else{el.textContent=text;if(onDone)onDone();}}
function skipTypewriter(){if(window.vTypewriter)vTypewriter.skip();}
function revealChoices(){const contentEl=document.getElementById('content');if(!contentEl)return;const rows=contentEl.querySelectorAll('.btn-row');rows.forEach(function(row,i){row.style.opacity='0';row.style.transform='translateY(6px)';row.style.transition='opacity 0.25s ease, transform 0.25s ease';setTimeout(function(){row.style.opacity='1';row.style.transform='translateY(0)';},80+i*80);});}
function extractDialogueText(rawText){const lines=rawText.split('\n');let bodyStart=0;for(let i=0;i<lines.length&&i<8;i++){let raw=lines[i].trim();raw=raw.replace(/[\u200B-\u200D\uFEFF]/g,'').trim();const t=raw.replace(/<[^>]+>/g,'').trim();if(!t||/^[═━⚜💎\u2550\u2501\s\uFE0F]+$/.test(t)){bodyStart=i+1;continue;}
if(/\d{2}:\d{2}\s*\|/.test(t)||(t.includes('Ano ')&&t.includes('|'))||/°C/.test(t)){bodyStart=i+1;continue;}
const stripped=t.replace(/[\[\]\(\)⭐✨💀⚔️🛡️⚜\uFE0F\s]/g,'');if(stripped.length>2&&stripped===stripped.toUpperCase()&&!/[a-z]/.test(stripped)){bodyStart=i+1;continue;}
break;}
let body=lines.slice(bodyStart);while(body.length>0){const last=body[body.length-1].replace(/<[^>]+>/g,'').trim();if(!last||/^[═━⚜💎\u2550\u2501\s\uFE0F\u2800]+$/.test(last)){body.pop();continue;}
break;}
return body.join('\n').trim();}
function applyMoodAmbient(mood){const g=MOOD_GRADIENTS[mood]||MOOD_GRADIENTS.neutral;document.body.style.background=g;document.body.style.backgroundAttachment='fixed';}
function clearMoodAmbient(){document.body.style.background='';document.body.style.backgroundAttachment='';}